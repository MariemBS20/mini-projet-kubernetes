apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc  
  labels:
    app: mysql
    type: local
    tier: database
spec:
  resources:
    requests:
      storage: 10Gi   
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: manual 